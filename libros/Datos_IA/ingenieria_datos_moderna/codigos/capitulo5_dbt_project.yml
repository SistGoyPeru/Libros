# Capítulo 5: dbt_project.yml

name: 'techstore_dbt'
version: '1.0.0'
config-version: 2

profile: 'techstore_dbt'

model-paths: ["models"]
test-paths: ["tests"]
seed-paths: ["seeds"]
macro-paths: ["macros"]

models:
  techstore_dbt:
    staging:
      +materialized: view
      +schema: staging
    marts:
      +materialized: table
      +schema: marts
